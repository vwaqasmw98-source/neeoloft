import { SupportClient } from './SupportClient';

export const metadata = { title: 'Support · Neeoloft', robots: { index: false, follow: false } };

export default function DashboardSupportPage() {
  return <SupportClient />;
}
